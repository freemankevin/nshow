# NShow - 视频搜索平台

一个本地运行的视频搜索工具，用于聚合和搜索外部视频网站的资源。

## 项目简介

NShow 是一个简洁的视频搜索前端工具，提供现代化的深色主题界面，支持：

- 🔍 视频搜索功能
- 🎬 按类型、年份筛选
- 📊 多种排序方式（最相关、最新、最高评分）
- 📄 分页浏览
- 🎯 视频详情查看
- ▶️ 一键播放（跳转到外部视频网站）

## 技术栈

### 后端
- **Python 3.8+**
- **Flask** - Web框架
- **Flask-CORS** - 跨域支持
- **Requests** - HTTP请求库

### 前端
- **React 18** - UI框架
- **Vite** - 构建工具
- **CSS3** - 深色主题样式

## 项目结构

```
nshow/
├── backend/                 # 后端目录
│   ├── api/                # API路由
│   │   ├── __init__.py
│   │   └── routes.py      # API接口定义（调用外部视频网站）
│   ├── app.py             # Flask应用主文件
│   └── requirements.txt   # Python依赖
├── frontend/              # 前端目录
│   ├── src/
│   │   ├── components/   # React组件
│   │   │   ├── VideoSearch.jsx  # 主搜索页面
│   │   │   ├── VideoCard.jsx    # 视频卡片组件
│   │   │   └── VideoModal.jsx   # 详情模态框
│   │   ├── App.jsx       # 主应用组件
│   │   ├── main.jsx      # 入口文件
│   │   ├── App.css       # 应用样式
│   │   └── index.css     # 全局样式（深色主题）
│   ├── index.html        # HTML模板
│   ├── package.json      # Node依赖
│   └── vite.config.js    # Vite配置
└── README.md             # 项目说明
```

## 安装和运行

### 前置要求

- Python 3.8 或更高版本
- Node.js 16 或更高版本
- npm 或 yarn

### 1. 安装后端依赖

```bash
cd backend
pip install -r requirements.txt
```

### 2. 配置外部API

编辑 [`backend/api/routes.py`](backend/api/routes.py) 文件，配置您要调用的外部视频网站API：

```python
# 外部视频网站API配置
EXTERNAL_API_BASE_URL = "https://api.example.com/videos"  # 替换为实际的API地址
API_KEY = ""  # 如果需要API密钥
```

然后取消注释 `call_external_api` 函数中的真实API调用代码，删除模拟数据部分。

### 3. 启动后端服务

在 `backend` 目录下运行：

```bash
python app.py
```

后端服务将在 `http://localhost:5000` 启动

### 4. 安装前端依赖

```bash
cd frontend
npm install
```

### 5. 启动前端服务

在 `frontend` 目录下运行：

```bash
npm run dev
```

前端服务将在 `http://localhost:3000` 启动

### 6. 访问应用

打开浏览器访问 `http://localhost:3000` 即可使用NShow。

## 功能说明

### 搜索功能
- 输入关键词搜索视频
- 支持搜索标题、演员、导演等

### 筛选功能
- **类型筛选**: 动作、喜剧、剧情、恐怖、爱情、科幻
- **年份筛选**: 2024、2023、2022、2021、2020、2019

### 排序功能
- **最相关**: 按热度排序
- **最新**: 按年份排序
- **最高评分**: 按评分排序

### 视频卡片
- 显示视频缩略图
- 显示标题、年份、类型
- 显示观看次数和评分
- 悬停显示播放按钮

### 详情模态框
- 显示视频大图
- 显示完整信息（年份、类型、评分）
- 显示剧情简介
- 提供播放和关闭按钮

### 分页
- 支持多页浏览
- 显示当前页码
- 快速跳转到指定页

## API接口

### 搜索视频
```
GET /api/search
```

查询参数：
- `q`: 搜索关键词（必填）
- `page`: 页码（默认1）
- `page_size`: 每页数量（默认20）
- `genre`: 类型筛选
- `year`: 年份筛选
- `sort`: 排序方式（relevance/newest/rating）

### 获取视频详情
```
GET /api/video/<id>
```

### 获取播放链接
```
GET /api/video/<id>/play
```

## 配置外部API

项目目前使用模拟数据，要接入真实的视频网站API，需要修改 [`backend/api/routes.py`](backend/api/routes.py)：

1. 在文件顶部配置API地址和密钥
2. 取消注释 `call_external_api` 函数中的真实API调用代码
3. 删除模拟数据部分
4. 根据实际API返回格式调整数据处理逻辑

## 开发说明

### 后端开发

- API路由定义在 [`backend/api/routes.py`](backend/api/routes.py)
- Flask应用配置在 [`backend/app.py`](backend/app.py)

### 前端开发

- 主搜索页面在 [`frontend/src/components/VideoSearch.jsx`](frontend/src/components/VideoSearch.jsx)
- 视频卡片组件在 [`frontend/src/components/VideoCard.jsx`](frontend/src/components/VideoCard.jsx)
- 详情模态框在 [`frontend/src/components/VideoModal.jsx`](frontend/src/components/VideoModal.jsx)
- 全局样式在 [`frontend/src/index.css`](frontend/src/index.css)

### 添加新功能

1. 后端：在 [`backend/api/routes.py`](backend/api/routes.py) 添加新的API端点
2. 前端：在 [`frontend/src/components/VideoSearch.jsx`](frontend/src/components/VideoSearch.jsx) 中调用新API

## 常见问题

### 如何接入真实的视频网站API？

编辑 [`backend/api/routes.py`](backend/api/routes.py)，取消注释真实API调用代码，并根据实际API文档调整参数和返回数据处理。

### 前端无法连接后端？

确保：
1. 后端服务正在运行（`http://localhost:5000`）
2. 前端配置了正确的代理（查看 [`frontend/vite.config.js`](frontend/vite.config.js)）

### 如何修改主题颜色？

编辑 [`frontend/src/index.css`](frontend/src/index.css) 中的CSS变量：

```css
:root {
  --primary: #ff6b35;      /* 主色调 */
  --secondary: #00d9ff;    /* 次要色调 */
  --bg-dark: #0a0e27;      /* 深色背景 */
  --bg-card: #1a1f3a;      /* 卡片背景 */
  /* ... */
}
```

### 如何添加更多筛选选项？

在 [`frontend/src/components/VideoSearch.jsx`](frontend/src/components/VideoSearch.jsx) 中添加新的筛选器，并传递给后端API。

## 许可证

MIT License

## 贡献

欢迎提交Issue和Pull Request！

## 联系方式

如有问题或建议，请提交Issue。
